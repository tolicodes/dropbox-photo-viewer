describe("@cloud-photo-viewer/dropbox-api-scraper/createDropboxTokenFile", () => {
  it.todo("should generate a token using oauth-cli and save it to a file");
  // read `id` arg
  // use `@node-cli-toolkit/oauth-cli` to log in
  // save token using `@node-api-toolkit/save-token` to a file
  // with the `id` arg as the name in `/tmp`
});
